
  # Cashier UX Flow

  This is a code bundle for Cashier UX Flow. The original project is available at https://www.figma.com/design/ith4BKr7fSY5M7F8eBd34t/Cashier-UX-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # SchoolStoreCasher
# SchoolStoreCasher
# SchoolStoreCasher
